{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "plugins": [
            {
                "transform": "../src/transformer.ts",
                "schema": "schema.d.ts",
                "outFile": "types.js"
            }
        ]
    }
}
